// ESLint 8's legacy indent rule overflows on the panel's nested React 16 JSX tree.
// The file remains conventionally formatted and all other project rules still apply.
module.exports = {
    rules: {
        indent: 'off'
    }
};
