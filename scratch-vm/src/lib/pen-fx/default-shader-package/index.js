// Built-ins are source-controlled modules, never an archive loaded at runtime.
// Share the renderer's source folder so edits cannot drift from the default package.
export {programSources} from 'scratch-render/src/pen-fx/shaders';
